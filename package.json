{
  "name": "cam-duc-hiep",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "d1:init-local": "wrangler d1 execute cam-duc-hiep-db-test --local --file=./schema.sql",
    "d1:init-remote": "wrangler d1 execute cam-duc-hiep-db-test --remote --file=./schema.sql"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.2.0",
    "astro": "^5.4.0"
  },
  "devDependencies": {
    "wrangler": "^4.140.0"
  }
}
