---
import { films } from '../data/films';

interface Props {
  title: string;
  description?: string;
  activeSlug?: string;
  activeNav?: 'films' | 'about' | 'cv';
}

const { 
  title, 
  description = "Cầm Đức Hiệp - Filmmaker Portfolio", 
  activeSlug,
  activeNav = 'films'
} = Astro.props;

const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Cầm Đức Hiệp</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={`${title} | Cầm Đức Hiệp`} />
    <meta property="og:description" content={description} />
    
    <!-- Google Fonts: Inter & Fraunces -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,500;1,9..144,400&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style is:global>
      :root {
        --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
        --font-serif: 'Fraunces', Georgia, serif;
        --color-bg: #ffffff;
        --color-text: #000000;
        --color-muted: #666666;
      }
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }
      html, body {
        background-color: #ffffff;
        color: #000000;
        font-family: var(--font-sans);
        min-height: 100vh;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        line-height: 1.6;
      }
      a {
        color: #000000;
        text-decoration: none;
      }
      /* Clean minimal scrollbar */
      ::-webkit-scrollbar {
        width: 6px;
        height: 6px;
      }
      ::-webkit-scrollbar-track {
        background: #ffffff;
      }
      ::-webkit-scrollbar-thumb {
        background: #e0e0e0;
        border-radius: 3px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: #bbbbbb;
      }
    </style>
  </head>
  <body>
    <div style="display: flex; min-height: 100vh; width: 100%; background: #ffffff;">
      <!-- Left Sidebar (No border, clean spacing) -->
      <aside style="width: 280px; flex-shrink: 0; padding: 75px 40px 75px 70px; position: sticky; top: 0; height: 100vh; overflow-y: auto; display: flex; flex-direction: column; justify-content: space-between; background: #ffffff;">
        <nav style="display: flex; flex-direction: column;">
          <!-- Films Section Header -->
          <div style="margin-bottom: 0.65rem;">
            <a 
              href="/" 
              style={`font-size: 1rem; font-weight: 600; color: #000000; letter-spacing: -0.01em; transition: opacity 0.2s; ${currentPath === '/' ? 'opacity: 0.35;' : 'opacity: 1;'}`}
            >
              Films
            </a>
          </div>

          <!-- Film Submenu (Active item is light gray #cccccc as in reference, inactive is black #000000) -->
          <ul style="list-style: none; display: flex; flex-direction: column; gap: 0.5rem; padding-left: 0; margin-bottom: 2.25rem;">
            {films.map((film) => {
              const isActive = currentPath === `/films/${film.slug}`;
              return (
                <li>
                  <a 
                    href={`/films/${film.slug}`} 
                    style={`font-size: 0.92rem; transition: opacity 0.15s ease; display: block; font-weight: 400; ${
                      isActive 
                        ? 'color: #000000; opacity: 0.35;' 
                        : 'color: #000000; opacity: 1;'
                    }`}
                    onmouseover="this.style.opacity='0.6';"
                    onmouseout={isActive ? "this.style.opacity='0.35';" : "this.style.opacity='1';"}
                  >
                    {film.title}
                  </a>
                </li>
              );
            })}
          </ul>

          <!-- About & CV Links -->
          <div style="display: flex; flex-direction: column; gap: 0.65rem;">
            <a 
              href="/about" 
              style={`font-size: 1rem; font-weight: 600; color: #000000; letter-spacing: -0.01em; transition: opacity 0.15s ease; ${
                currentPath === '/about' 
                  ? 'opacity: 0.35;' 
                  : 'opacity: 1;'
              }`}
              onmouseover="this.style.opacity='0.6';"
              onmouseout={currentPath === '/about' ? "this.style.opacity='0.35';" : "this.style.opacity='1';"}
            >
              About
            </a>

            <a 
              href="/cv" 
              style={`font-size: 1rem; font-weight: 600; color: #000000; letter-spacing: -0.01em; transition: opacity 0.15s ease; ${
                currentPath === '/cv' 
                  ? 'opacity: 0.35;' 
                  : 'opacity: 1;'
              }`}
              onmouseover="this.style.opacity='0.6';"
              onmouseout={currentPath === '/cv' ? "this.style.opacity='0.35';" : "this.style.opacity='1';"}
            >
              CV
            </a>
          </div>
        </nav>

        <!-- Admin link -->
        <div style="padding-top: 2rem;">
          <a 
            href="/admin" 
            target="_blank" 
            style="font-size: 0.75rem; color: #000000; opacity: 0.2; transition: opacity 0.2s; text-decoration: none;"
            onmouseover="this.style.opacity='0.6';"
            onmouseout="this.style.opacity='0.2';"
          >
            ● Admin CMS
          </a>
        </div>
      </aside>

      <!-- Main Content Area -->
      <main style="flex: 1; min-width: 0; padding: 75px 60px 100px 30px; background: #ffffff;">
        <slot />
      </main>
    </div>

    <!-- Responsive Mobile Bar -->
    <style>
      @media (max-width: 960px) {
        div[style*="display: flex"] {
          flex-direction: column !important;
        }
        aside {
          width: 100% !important;
          height: auto !important;
          position: static !important;
          padding: 30px 24px !important;
        }
        main {
          padding: 24px 20px 60px !important;
        }
      }
    </style>
  </body>
</html>
